# ~/.tmuxinator/home.yml
# some windows I tend to use at home
project_name: Home
project_root: ~/
tabs:
  - home:
  - dotfiles: cd. && gs
  - brew: brew update
  - howto: cd Dropbox/HowTo && vim
  - servers:
      layout: even-horizontal
      panes:
        - ursa
        - fornax
